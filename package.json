{
  "name": "css-study",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "compile:sass": "npm run compile:sass --workspace=packages/scss-theme",

    "compile:sass:information-box": "npm run compile:sass --workspace=apps/information-box",
    "build:information-box": "npm run build --workspace=apps/information-box",
    "start:information-box": "npm run start --workspace=apps/information-box",
  
    "build:float-test": "npm run build --workspace=apps/float-test",
    "start:float-test": "npm run start --workspace=apps/float-test",

    "build:positioning-test": "npm run build --workspace=apps/positioning-test",
    "start:positioning-test": "npm run start --workspace=apps/positioning-test",

    "build:flexbox-test": "npm run build --workspace=apps/flexbox-test",
    "start:flexbox-test": "npm run start --workspace=apps/flexbox-test",

    "build:grid-test": "npm run build --workspace=apps/grid-test",
    "start:grid-test": "npm run start --workspace=apps/grid-test",
    
    "build:rwd-media-queries-test": "npm run build --workspace=apps/rwd-media-queries-test",
    "start:rwd-media-queries-test": "npm run start --workspace=apps/rwd-media-queries-test",

    "build:navbar-demo": "npm run build --workspace=apps/navbar-demo",
    "start:navbar-demo": "npm run start --workspace=apps/navbar-demo" 
  }
}